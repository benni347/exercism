from math import sqrt

def square_root(number):
    """
    This function calculated the natural root of the given number.

    E. g:
    square_root(25) -> 5

    @number: int to be calculated sqrt number
    @return: int the sqrt of @number.
    """
    return sqrt(number)
